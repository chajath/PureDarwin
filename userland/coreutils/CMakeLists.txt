cmake_minimum_required(VERSION 3.20)

# Placeholder for GNU coreutils cross-compilation.
# In practice, coreutils is built via the bootstrap script (Makefile-based).
# This CMakeLists exists for project structure visibility.

message(STATUS "Coreutils: use ../scripts/build-coreutils.sh to build")
